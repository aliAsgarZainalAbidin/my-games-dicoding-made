# This config was automatically generated from your source code
# Stacks detected: deps:java:.,tool:gradle:
version: 2.1

#orbs:
#  android: circleci/android@0.2.1
#  environment:
#    JAVA_HOME: /usr/lib/jvm/java-17-openjdk-amd64  # Set JAVA_HOME ke Java 17
#    GRADLE_OPTS: '-Dorg.gradle.jvmargs=-Xmx4096m -Dorg.gradle.daemon=false'
executors:
  android-executor:
    docker:
      - image: circleci/android:api-30  # or change to api-31
    environment:
      JAVA_HOME: /usr/lib/jvm/java-17-openjdk-amd64  # Set JAVA_HOME to Java 17
      GRADLE_OPTS: '-Dorg.gradle.jvmargs=-Xmx4096m -Dorg.gradle.daemon=false'

jobs:
  build:
    executor: android-executor
    steps:
      - checkout
      - run:
          name: Setup Java 17
          command: |
            sudo apt update
            sudo apt install -y software-properties-common
            sudo add-apt-repository ppa:openjdk-r/ppa -y
            sudo apt update
            sudo apt install -y openjdk-17-jdk
            java -version  # Verify Java version
      - run:
          name: Build
          command: ./gradlew assembleDebug
      - restore_cache:
          key: android-orb-v1-
      - run:
          name: Chmod permissions
          command: sudo chmod +x ./gradlew
      - run:
          name: Download Dependencies
          command: ./gradlew androidDependencies
      - save_cache:
          key: 'android-orb-v1-{{ epoch }}'
          paths:
            - ~/.android/build-cache
            - ~/.android/cache
      - run:
          name: Run Build
          command: ./gradlew build
      - store_artifacts:
          path: app/build/reports
          destination: reports
      - run:
          name: Run Tests
          command: ./gradlew lint test
      - store_test_results:
          path: app/build/test-results
      - store_artifacts:
          path: app/build/outputs/apk/debug/
          destination: artifact-file
workflows:
  build-and-test:
    jobs:
      - build
    # - deploy:
    #     requires:
    #       - test-java
